import PlaceholderPage from "./shared/PlaceholderPage.jsx";

export default function ImageTools() {
  return (
    <PlaceholderPage
      title="Image Tools"
      description="A future section for lightweight image utilities that can run in the browser."
      ideas={["Resize images", "Convert formats", "Compress assets", "Simple mood boards"]}
    />
  );
}
