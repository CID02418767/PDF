# Archived FastAPI Backend

This folder contains the previous FastAPI implementation of the PDF tool.

It is kept only as reference. The active project is now a pure frontend Vite + React app that processes PDF files locally in the browser with `pdf-lib`.

The archived backend is not required for local development, static hosting, GitHub Pages, or browser-based PDF processing.
